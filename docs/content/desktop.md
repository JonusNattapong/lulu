# Desktop Integration

Lulu is designed to eventually break out of the terminal. 

While currently a CLI tool, the architecture of the Core Systems is decoupled from the `ink` UI renderer. This allows future integration into Electron or Tauri-based desktop wrappers, enabling global hotkeys, floating windows, and system-wide accessibility similar to Apple Intelligence or Windows Copilot.
